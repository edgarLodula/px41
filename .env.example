# ============================================================
# Variáveis de ambiente do projeto px41
# Copie para .env e preencha com seus valores reais.
# NUNCA commite o .env com valores reais.
# ============================================================

# --- OpenAI ---
OPENAI_API_KEY=sk-proj-...
OPENAI_MODEL=gpt-4o-mini

# Geração das imagens ilustrativas dos slides (opcional — sem OPENAI_API_KEY
# os slides ficam só com texto, como antes). gpt-image-1 é o padrão porque é
# o único modelo de imagem habilitado em algumas contas OpenAI (dall-e-2/3
# podem não existir mais pra sua chave — teste antes de trocar).
OPENAI_IMAGE_MODEL=gpt-image-1
# 1536x1024 (paisagem) casa melhor com a caixa larga/baixa da imagem no
# slide do que 1024x1024 (quadrada) — ela aparece bem maior, sem cortar nada.
OPENAI_IMAGE_SIZE=1536x1024

# --- HeyGen (geração de vídeos, opcional) ---
HEYGEN_API_KEY=
HEYGEN_AVATAR_ID=
HEYGEN_VOICE_ID=

# Ajustes finos de naturalidade do avatar (todos opcionais, com defaults sensatos):
# Engine: "avatar_iv" (padrão, suporta expressiveness) | "avatar_v" (qualidade maior, sem expressiveness)
HEYGEN_ENGINE=avatar_iv
# Velocidade da fala (0.5-1.5). Valores um pouco abaixo de 1.0 tendem a soar mais natural/pausado.
HEYGEN_SPEED=1.0
# Sotaque/idioma da voz.
HEYGEN_VOICE_LOCALE=pt-BR
# Expressividade padrão quando a cena não define uma direção própria: "low" | "medium" | "high"
HEYGEN_EXPRESSIVENESS=high
# Motion prompt genérico (fallback) quando a cena não tem [PRODUCAO] definido.
# Quanto mais detalhado, mais natural fica a gesticulação do avatar (gestos com as
# duas mãos, variação de ritmo, olhar, sorriso, etc. — evita movimento robótico/repetitivo).
HEYGEN_MOTION_PROMPT=warm, confident instructor with natural human body language: uses both hands expressively and fluidly to emphasize key points, open-palm gestures while explaining concepts, occasional finger-counting when listing items, subtle shifts in posture and weight between sentences, natural head tilts and nods on important words, expressive eyebrows and genuine smiles, brief thoughtful pauses with eyes slightly raised before continuing, frequent natural eye contact with the camera, relaxed shoulders, varied gesture timing and amplitude so the movement never feels repetitive or robotic — like a real teacher speaking spontaneously

# --- Caminhos de dependências externas ---
# Deixe vazio para usar detecção automática pelo PATH
TESSERACT_PATH=
WKHTMLTOPDF_PATH=

# --- Gemini ---
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# --- CORS ---
FRONTEND_ORIGIN=http://localhost:8080

# --- Logging ---
LOG_LEVEL=INFO

# --- Comportamento do pipeline ---
# Política de vocabulário proibido: warn | fail | retry
POLITICA_VOCABULARIO=warn

# Delays entre chamadas de API (segundos, use 0 em testes)
DELAY_ENTRE_TOPICOS=10
DELAY_ENTRE_SECOES=10
DELAY_ENTRE_DISCIPLINAS=35
